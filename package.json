{
  "name": "racing-web",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "build": "npm run build --workspace client && npm install --workspace server",
    "start": "node server/Server.js"
  },
  "engines": {
    "node": "20.x"
  }
}