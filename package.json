{
  "name": "racing-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "node server/Server.js"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {}
}
